import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Hola-Mundo-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
